package dev.eriksonn.aeronautics.content.blocks.hot_air.envelope;

import net.minecraft.world.item.DyeColor;

// Compile-only stub — excluded from the output JAR by build.gradle.
// At runtime, Aeronautics provides the real class at this package path.
public interface Envelope {
    DyeColor getColor();
}
